<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:layout_marginStart="8dp"
    android:layout_marginEnd="8dp"
    android:layout_marginBottom="4dp"
    android:background="@drawable/card_info_panel_bg">

    <!-- Checkbox (visible solo en modo selección) -->
    <CheckBox
        android:id="@+id/cancionCheckbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:clickable="false"
        android:focusable="false"
        android:layout_marginEnd="8dp"
        android:buttonTint="#FFD700"/>

    <!-- Indicador canción activa -->
    <TextView
        android:id="@+id/cancionActiva"
        android:layout_width="20dp"
        android:layout_height="wrap_content"
        android:text="▶"
        android:textColor="#FFD700"
        android:textSize="12sp"
        android:gravity="center"
        android:visibility="gone"
        android:layout_marginEnd="10dp"/>

    <!-- Ícono nota -->
    <TextView
        android:layout_width="20dp"
        android:layout_height="wrap_content"
        android:text="🎵"
        android:textSize="12sp"
        android:gravity="center"
        android:layout_marginEnd="10dp"/>

    <!-- Nombre -->
    <TextView
        android:id="@+id/cancionNombre"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="#DDDDFF"
        android:textSize="14sp"
        android:maxLines="1"
        android:ellipsize="end"/>

    <!-- Botón eliminar -->
    <TextView
        android:id="@+id/btnEliminarCancion"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:text="🗑"
        android:textSize="16sp"
        android:gravity="center"
        android:layout_marginStart="8dp"/>

</LinearLayout>