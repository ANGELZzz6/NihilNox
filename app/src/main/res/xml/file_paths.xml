<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Almacenamiento interno de la app (filesDir) — fotos de metas y avatares -->
    <files-path name="app_files" path="."/>
    <files-path name="ocr_images" path="." />
    <!-- Almacenamiento externo privado — PDFs exportados -->
    <external-files-path name="documents" path="Documents/"/>
</paths>